# 依赖说明：
# - Streamlit Cloud 请在 Advanced settings 选择 Python 3.11 或 3.12
# - TensorFlow 仅在 Python < 3.13 时安装（3.14 尚无官方 wheel）
# - 选对 Python 后，LSTM / GRU / ALSTM / Transformer 等时序模型均可使用

akshare>=1.14.0; python_version >= "3.9"
requests>=2.28.0
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
pyyaml>=6.0
matplotlib>=3.7.0
tabulate>=0.9.0
streamlit>=1.28.0
plotly>=5.18.0
scipy>=1.10.0
tqdm>=4.66.0

# 表格模型
xgboost>=2.0.0
catboost>=1.2.0; python_version >= "3.9" and python_version < "3.13"

# 时序深度学习（LSTM / GRU / ALSTM / Transformer / mlp_seq）
# 关键：必须在 Streamlit Advanced settings 选 Python 3.11 或 3.12
tensorflow>=2.15.0,<2.20.0; python_version >= "3.9" and python_version < "3.13"
